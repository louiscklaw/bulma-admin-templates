extends layout/layout-test.pug
include pug_inc/mixins.pug


block title
  title login

block append content
  .column.is-9
    section.section
      .container
        +breadcrumb(['pages','login'])

        h1.subtitle.is-1 login
